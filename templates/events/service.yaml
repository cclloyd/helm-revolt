apiVersion: v1
kind: Service
metadata:
  name: {{ include "stoat.fullname" . }}-events
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "stoat.labels" . | nindent 4 }}
    stoat-app: events
  {{- with (merge (.Values.global.all.service.annotations | default dict) (.Values.events.service.annotations | default dict)) }}
  annotations:
    {{- toYaml . | nindent 8 }}
  {{- end }}
spec:
  type: {{ .Values.events.service.type }}
  ports:
    - name: http
      targetPort: http
      protocol: TCP
      port: {{ .Values.global.events.port }}
  selector:
    {{- include "stoat.selectorLabels" . | nindent 4 }}
    stoat-app: events

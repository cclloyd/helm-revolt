apiVersion: v1
kind: Service
metadata:
  name: {{ include "stoat.fullname" . }}-voice
  namespace: {{ .Values.global.namespace }}
  labels:
    {{- include "stoat.labels" . | nindent 4 }}
    stoat-app: voice-ingress
  {{- with (merge (.Values.global.all.service.annotations | default dict) (.Values.voice.service.annotations | default dict)) }}
  annotations:
    {{- toYaml . | nindent 8 }}
  {{- end }}
spec:
  type: {{ .Values.voice.service.type }}
  ports:
    - name: http
      targetPort: http
      protocol: TCP
      port: {{ .Values.global.voice.port }}
  selector:
    {{- include "stoat.selectorLabels" . | nindent 4 }}
    stoat-app: voice-ingress
